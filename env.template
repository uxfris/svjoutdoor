# Supabase Configuration
# Get these values from your Supabase project dashboard
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App Configuration
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_SITE_URL=

# Auth Redirects
# Ensure this URL is allowed in Supabase Auth settings (Site URL and Redirect URLs)
# Example: https://your-domain.com or http://localhost:3000
# The app uses /auth/callback to complete email confirmations and magic links
# After confirmation, users are redirected to /dashboard by default

# Optional: For production deployment
# NEXT_PUBLIC_APP_URL=
# NEXT_PUBLIC_SITE_URL=
